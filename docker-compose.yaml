version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: frontend
    container_name: frontend_cont
    ports:
      - "3000:3000"
    networks:
      - travelAI_network
    restart: always

networks:
  travelAI_network:
    external: true
